[target.'cfg(all())']
rustflags = ["-C", "linker=dylint-link", "-A", "linker_messages"]

# For Rust versions 1.74.0 and onward, the following alternative can be used
# (see https://github.com/rust-lang/cargo/pull/12535):
# linker = "dylint-link"