{
	"name": "sapa-tv-ru",
	"version": "0.1.0",
	"description": "",
	"scripts": {
		"dev": "cargo run -p gen-openapi -- generated/openapi.json && dotenv -e .env.dev -- concurrently -k -n backend,frontend \"cargo run -p backend\" \"pnpm --filter frontend dev\"",
		"format": "prettier --write . --log-level warn && cargo fmt",
		"format:check": "prettier --check . && cargo fmt --check"
	},
	"keywords": [],
	"author": "Th0rN13",
	"license": "MIT",
	"packageManager": "pnpm@11.21.0",
	"devDependencies": {
		"@ast-grep/cli": "^0.45.0",
		"concurrently": "^10.0.4",
		"dotenv-cli": "^11.0.0",
		"prettier": "^3.9.6",
		"prettier-plugin-svelte": "^4.1.1",
		"turbo": "^2.10.9"
	}
}
